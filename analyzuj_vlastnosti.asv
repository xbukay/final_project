function analyzuj_vlastnosti(cit, men)

    if length(men) == length(cit)
        disp('System je rydzy');
    elseif length(men)>length(cit)
        disp('System nie je striktne rydzy');
    else
        disp(
    end
end